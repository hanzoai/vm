# Runs on the Hanzo Git act_runners (git.hanzo.ai); labels from
# hanzoai/.github/RUNNERS.md. The workspace's default members exclude vm-linux,
# so the Linux leg checks it by name.
name: ci
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  macos:
    # hanzo-build-darwin-arm64: the label RUNNERS.md registers for the
    # Apple pool. hanzo-build-macos-arm64 is not one it lists, and a job
    # asking for a label no runner advertises queues forever rather than
    # failing — so the repo keeps looking like it has CI on this leg.
    runs-on: hanzo-build-darwin-arm64
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - run: cargo fmt --all -- --check
      - run: cargo clippy --all-targets -- -D warnings
      - run: cargo test

  linux:
    runs-on: hanzo-build-linux-arm64
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo check -p vm-linux
