[package]
name = "shuru-proto"
version = "0.3.0"
edition = "2021"
description = "Shared wire protocol for shuru host/guest communication"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
