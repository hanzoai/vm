[package]
name = "vm-proxy"
version = "2.0.1"
edition = "2021"
description = "Transparent network proxy for Hanzo VM microVMs"
license = "Apache-2.0"
repository = "https://github.com/hanzoai/vm"

[dependencies]
smoltcp = { version = "0.12", default-features = false, features = [
    "alloc",
    "medium-ethernet",
    "proto-ipv4",
    "socket-tcp",
    "socket-udp",
] }
tokio = { version = "1", features = ["rt-multi-thread", "net", "io-util", "sync", "macros", "time", "process"] }
tokio-rustls = "0.26"
rustls = { version = "0.23", default-features = false, features = ["aws_lc_rs", "logging", "tls12"] }
rcgen = "0.13"
webpki-roots = "1"
boring = "4"
tokio-boring = "4"
simple-dns = "0.9"
lru = "0.14"
httparse = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
time = { version = "0.3", features = ["parsing", "formatting"] }
anyhow = "1"
tracing = "0.1"
libc = "0.2"
