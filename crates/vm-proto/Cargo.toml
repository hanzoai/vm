[package]
name = "vm-proto"
version = "0.1.1"
edition = "2021"
description = "Shared wire protocol for Hanzo VM host/guest communication"
license = "Apache-2.0"
repository = "https://github.com/hanzoai/vm"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
